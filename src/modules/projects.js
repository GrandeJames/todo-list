import { Project } from "../components/project";

// TODO: these will contain an array of task objects
let inbox = new Project("Inbox"); // Will contain all the tasks except custom projects
let today = new Project("Today"); // Will contain all tasks due today
